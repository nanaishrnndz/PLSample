
# Traffic Quest: Strategy Guild Board

This fixed build includes:
- quest types
- auto-suggest assignments
- dungeon weeks
- retro 8-bit styling

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```
